Tester l'endpoint API suivant avec Playwright MCP ou curl :

1. Vérifier que l'endpoint répond avec le bon status code
2. Vérifier que la réponse contient les champs attendus
3. Tester avec un tenant valide
4. Vérifier l'isolation tenant (pas de données cross-tenant)
5. Signaler toute anomalie

Endpoint à tester : $ARGUMENTS
